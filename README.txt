Libraries Used:
	Numpy


Instructions:
	1. To run the file in Ubuntu, open a terminal and enter "python 8_puzzle_solver.py" or "python3 8_puzzle_solver.py".
	2. The output will be shown on the terminal and the files will be saved in the same folder as the python file.

(The code can also be run in any IDE such as PyCharm or Visual Studio Code)


Testing:
	1. Input can be provided on line 192 by changing the matrix to desired initital state.
	2. Goal state can be changed at line 195.